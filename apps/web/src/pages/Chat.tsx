import { ChatWindow } from '../components/chat/ChatWindow';

export default function Chat() {
  return (
    <div className="h-[calc(100vh-7rem)] lg:h-[calc(100vh-5rem)] -m-4 lg:-m-6">
      <ChatWindow />
    </div>
  );
}
