[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head || echo 'Migration failed, continuing...' && uvicorn src.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
